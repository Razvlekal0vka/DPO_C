#include <stdio.h>

int main() {
    int a, b;

    // Ввод трех целых чисел
    scanf("%d %d", &a, &b);

    // Вычисление суммы
    int r = a - b;

    // Вывод результата в требуемом формате
    printf("%d\n", r);

    return 0;
}
